
[sky]
  name       = sky0                     # Name of sky scene.
  loglevel   = 0                        # 0: no log, 1: single summary, 2: full report.

[Background]
  gradient   = 5                        # % over 1 arcmin (JWST component only).
  pa         = 45                       # position angle of gradient (background increasing towards PA).
  centreFOV  = 0 0                      # centre of FOV.
  level      = low                      # Background with the G-component of the model included 'high' or missing 'low'.
  fast       = 0                        # Use or not the 2.5D speed up when flux(RA, DEC, WAV) = flux1(RA, DEC) * flux2(WAV).

[point_1]
  Type       = Point                    # Type of target.
  Cen        = 40.81 0                  # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_2]
  Type       = Point                    # Type of target.
  Cen        = 76.01 -1.87              # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_3]
  Type       = Point                    # Type of target.
  Cen        = 36.5 0                   # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_4]
  Type       = Point                    # Type of target.
  Cen        = 34.7136 11.2791          # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_5]
  Type       = Point                    # Type of target.
  Cen        = 29.5291 21.4542          # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_6]
  Type       = Point                    # Type of target.
  Cen        = 21.4542 29.5291          # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_7]
  Type       = Point                    # Type of target.
  Cen        = 11.2791 34.7136          # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_8]
  Type       = Point                    # Type of target.
  Cen        = 2.23498e-15 36.5         # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_9]
  Type       = Point                    # Type of target.
  Cen        = -11.2791 34.7136         # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_10]
  Type       = Point                    # Type of target.
  Cen        = -21.4542 29.5291         # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_11]
  Type       = Point                    # Type of target.
  Cen        = -29.5291 21.4542         # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_12]
  Type       = Point                    # Type of target.
  Cen        = -34.7136 11.2791         # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_13]
  Type       = Point                    # Type of target.
  Cen        = -36.5 4.46996e-15        # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_14]
  Type       = Point                    # Type of target.
  Cen        = -34.7136 -11.2791        # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_15]
  Type       = Point                    # Type of target.
  Cen        = -29.5291 -21.4542        # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_16]
  Type       = Point                    # Type of target.
  Cen        = -21.4542 -29.5291        # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_17]
  Type       = Point                    # Type of target.
  Cen        = -11.2791 -34.7136        # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_18]
  Type       = Point                    # Type of target.
  Cen        = -6.70494e-15 -36.5       # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_19]
  Type       = Point                    # Type of target.
  Cen        = 11.2791 -34.7136         # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_20]
  Type       = Point                    # Type of target.
  Cen        = 21.4542 -29.5291         # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_21]
  Type       = Point                    # Type of target.
  Cen        = 29.5291 -21.4542         # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[point_22]
  Type       = Point                    # Type of target.
  Cen        = 34.7136 -11.2791         # Where to place the target (arcsec offsets from centreFOV).

  [[sed]]
    Type       = BB                     # Type of spectral energy distribution (e.g. BlackBody spectrum).
    Temp       = 10000                  # Representative temperature for the blackbody.
    wref       = 10                     # {optional} reference wavelength (in micron).
    flux       = 700000                 # {optional} Reference flux (in microJy) for scaling the blackbody function.

[galaxy_1]
  Type       = Galaxy                   # Type of target.
  Cen        = 0 0                      # RA,DEC (offset, specified in arcseconds).
  n          = 1                        # Sersic index of the Galaxy.
  re         = 1                        # Effective radius (arcsec).
  q          = 0.4                      # Axial ratio.
  pa         = 0                        # position angle (deg).

  [[sed]]
    Type       = pysynphot              # specify SED from PySynphot librarires.
    family     = bkmodels               # SED Family from which to draw.
    sedname    = bk_b0005               # Template within catalogue to be used.
    wref       = 10                     # Reference wavelength for scaling (in micron).
    flux       = 100000                 # Reference flux for scaling (in microJy).

  [[velomap]]
    Type       = FlatDisk               # Type of Velocity map to initialise.
    Cen        = 0 0                    # Specify the centre of the disk (same as above in the spatial settings).
    vrot       = 200                    # Rotational Velocity (km/s).
    pa         = 0                      # Position angle of the velocity map (deg).
    q          = 0.4                    # Axial ratio of major and minor axes.
    c          = 0                      # measure of diskiness/boxiness (0 = purely elliptical).

  [[losvd]]
    Type       = ModGauss               # Specify the type of velocity distribution.
    sigma      = 200                    # Velocity dispersion of the target.
    h3         = 0                      # Multiplier of 3rd Hermite polynomial (0 = Gaussian).
    h4         = 0                      # Multiplier of 4th Hermite polynomial (0 = Gaussian).
